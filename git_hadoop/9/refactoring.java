/**
* Extract Class.
* The methods compareFs(), copyRemoteFiles(), copyLog4jPropertyFile(), 
* validateFilePath(), getPathURI(), copyJar() are extracted into the 
* new class JobResourceUploader.
* The method body of copyAndConfigureFiles() is extracted as the method
* uploadFiles of JobResourceUploader.
*/